{{!--**TODO: try to treat textarea in the same labeled-input --}}
<input type="{{type}}" id="{{primaryClass}}-{{id}}" {{#if additionalClasses}}class="{{additionalClasses}}"{{/if}} {{#if name}}name="{{name}}"{{/if}} {{#if value}}value="{{value}}"{{/if}} />